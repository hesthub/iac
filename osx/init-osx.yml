---
- name: Bootstrap OSX dev machine
  hosts: all

  vars_files:
    - default.config.yml

  roles:
    - role: homebrew
      tags: ["homebrew"]

  tasks:
    - name: Dotfiles
      import_tasks: tasks/dotfiles.yml
      tags: ["dotfiles"]
